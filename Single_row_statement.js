for(var i = 1; i <=10; i++){
  process.stdout.write(i + ' '); 
}
// How it works:
// i is a number (integer).
// ' ' is a string (a single space character).
// When you use the + operator between a number and a string, 
//JavaScript converts the number into a string and then concatenates them.